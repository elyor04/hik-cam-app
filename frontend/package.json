{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/check-stub-drift.mjs && vite",
    "build": "node scripts/check-stub-drift.mjs && tsc && vite build",
    "stub:install": "node scripts/install-preview-stub.mjs && node scripts/check-stub-drift.mjs --strict",
    "check:stub": "node scripts/check-stub-drift.mjs --strict",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.6.3",
    "vite": "^7.0.0"
  }
}